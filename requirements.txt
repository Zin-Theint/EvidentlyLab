# Fairness & Drift Lab (Full MLflow + UI)
# Recommended: Python 3.12 (pyarrow wheels available). Avoid Python 3.13.

numpy==2.3.4
pandas==2.3.3
scikit-learn==1.7.2
evidently==0.7.15

# Full MLflow (bundles the UI)
mlflow==2.16.2
pyarrow==17.0.0

# UI server deps (safe to include)
flask>=3,<4
gunicorn>=21,<24
